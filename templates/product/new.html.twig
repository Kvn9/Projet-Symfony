{% extends 'base.html.twig' %}

{% block title %}{{'product.new'|trans}}{% endblock %}

{% block body %}
    <h1>{{'product.create'|trans}}</h1>

    {{ form(add) }}

    <a href="{{ path('app_product_index') }}">{{'product.back'|trans}}</a>
{% endblock %}
