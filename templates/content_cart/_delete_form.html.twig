<form method="post" action="{{ path('app_content_cart_delete', {'id': content_cart.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ content_cart.id) }}">
    <button class="btn">Delete</button>
</form>
